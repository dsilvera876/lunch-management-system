# Deprecated — do not use

This directory previously contained an **Nginx** staging template.

The Lunch Management System staging deployment now uses **Apache 2** on Ubuntu.

Use instead:

- `deploy/apache/lunch-management-staging.conf`
- `deploy/STAGING.md`

The Nginx template was removed to prevent accidental use on servers that already run Apache.
